import*as Notifications from'expo-notifications';import{useRouter}from'expo-router';import{useEffect}from'react';
const ALLOWED=['/study/setup','/(tabs)/home'] as const;export function useNotificationResponseHandler(){const router=useRouter();useEffect(()=>{const open=(response:Notifications.NotificationResponse|null)=>{const url=response?.notification.request.content.data?.url;if(typeof url==='string'&&ALLOWED.includes(url as typeof ALLOWED[number]))router.push(url as '/study/setup')};Notifications.getLastNotificationResponseAsync().then(open);const sub=Notifications.addNotificationResponseReceivedListener(open);return()=>sub.remove()},[router])}
