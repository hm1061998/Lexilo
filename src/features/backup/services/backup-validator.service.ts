import { InvalidBackupError,UnsupportedBackupVersionError } from '@/shared/errors/app-error';
import { BACKUP_FORMAT,BACKUP_VERSION,type LexiloBackup } from '../types/backup.types';
export function stableStringify(value:unknown):string{if(Array.isArray(value))return `[${value.map(stableStringify).join(',')}]`;if(value&&typeof value==='object')return `{${Object.entries(value).filter(([k])=>k!=='checksum').sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>`${JSON.stringify(k)}:${stableStringify(v)}`).join(',')}}`;return JSON.stringify(value);}
export function checksumText(text:string){let hash=2166136261;for(let i=0;i<text.length;i++){hash^=text.charCodeAt(i);hash=Math.imul(hash,16777619);}return (hash>>>0).toString(16).padStart(8,'0');}
export function validateBackup(input:unknown):LexiloBackup{if(!input||typeof input!=='object')throw new InvalidBackupError('Tệp sao lưu không hợp lệ.');const b=input as Partial<LexiloBackup>;if(b.format!==BACKUP_FORMAT||!b.collections||typeof b.createdAt!=='number')throw new InvalidBackupError('Sai định dạng sao lưu Lexilo.');if(b.backupVersion!==BACKUP_VERSION)throw new UnsupportedBackupVersionError('Phiên bản sao lưu chưa được hỗ trợ.');const expected=checksumText(stableStringify(b));if(b.checksum!==expected)throw new InvalidBackupError('Checksum của tệp sao lưu không khớp.');for(const required of ['decks','cards','card_progress','learning_goals'])if(!Array.isArray(b.collections[required]))throw new InvalidBackupError(`Thiếu collection ${required}.`);return b as LexiloBackup;}
