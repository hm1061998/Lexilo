import{ScrollView,StyleSheet,Text,View}from'react-native';import*as Sharing from'expo-sharing';import{useBackupHistory,useCreateBackup}from'@/features/backup/hooks/use-backup';import{AppButton}from'@/shared/components/app-button';import{useAppTheme}from'@/shared/theme/use-app-theme';
export default function BackupScreen(){const{colors}=useAppTheme(),history=useBackupHistory(),create=useCreateBackup();return <ScrollView style={{backgroundColor:colors.background}} contentContainerStyle={s.screen}><Text style={[s.title,{color:colors.text}]}>Sao lưu cục bộ</Text><Text style={{color:colors.textMuted}}>Bản sao lưu chứa dữ liệu học, không chứa token hay binary media.</Text><AppButton label={create.isPending?'Đang tạo…':'Tạo và chia sẻ bản sao lưu'} disabled={create.isPending} onPress={()=>create.mutate(undefined,{onSuccess:async r=>{if(await Sharing.isAvailableAsync())await Sharing.shareAsync(r.uri,{mimeType:'application/json'})}})}/>{history.data?.map(x=><View key={x.id}><Text style={{color:colors.text}}>{x.file_name??'Backup'}</Text><Text style={{color:colors.textMuted}}>{new Date(x.created_at).toLocaleString()} · {x.status}</Text></View>)}</ScrollView>}const s=StyleSheet.create({screen:{padding:20,gap:16},title:{fontSize:28,fontWeight:'800'}});
